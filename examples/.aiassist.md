# Example .aiassist.md for your projects
# Copy this file to your project root and customize it

# My Project Name

## Tech Stack
- Python 3.12, FastAPI, PostgreSQL
- React + TypeScript (frontend)
- Docker + docker-compose (deployment)

## Architecture
- `src/api/` - FastAPI route handlers
- `src/models/` - SQLAlchemy models
- `src/services/` - Business logic layer
- `src/utils/` - Shared utilities
- `tests/` - Pytest test suite

## Conventions
- Use type hints everywhere
- Follow PEP 8 style guide
- Tests use pytest with async support via pytest-asyncio
- Commit messages follow conventional commits
- API endpoints use RESTful conventions

## Environment
- Python 3.12 required
- Set up with `pip install -e ".[dev]"`
- Run tests with `pytest`
- Environment variables in `.env` file
